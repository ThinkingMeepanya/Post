query Post($id: Int!) {
    post(id: $id) {
        id
        text
        createdAt
        updatedAt
        title
        points
        voteStatus
        creator {
        id
        username
    }
    }
}